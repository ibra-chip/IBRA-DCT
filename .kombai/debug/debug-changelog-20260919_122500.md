## Changes Made

### 1. Localized the user-removal confirmation dialog
- **File:** `script.js`
- **Change:** `loadUsers()` now builds the confirmation text from the active language. French uses `Supprimer l'utilisateur <name> ?`; Serbian/Bosnian keeps `Ukloniti korisnika <name>?`.
- **Reason:** The secondary remove-user button was already localized, but the native confirmation dialog remained in Serbian/Bosnian while the interface was in French.
- **Safety:** The existing `window.confirm` guard, `DELETE /api/users/:id` request, owner-only visibility, and self-delete protection were unchanged.

## Revert Status
- [ ] Revert the localized confirmation prompt in `script.js`
- [ ] Delete this changelog after reverting the code change
